# Anthropic API (если не задан — чатбот работает в mock-режиме)
ANTHROPIC_API_KEY=
LLM_MODEL=claude-haiku-4-5-20251001

# База данных (по умолчанию — SQLite, для prod — PostgreSQL)
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/turonbank

# CORS
FRONTEND_ORIGIN=http://localhost:5173

# iSpring LMS (mock-режим, если URL не задан)
ISPRING_BASE_URL=
ISPRING_API_KEY=

# Папка с регламентами для RAG
REGULATIONS_DIR=./data/regulations
